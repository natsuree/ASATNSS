<div class="asat-password-hint" data-password-strength-panel>
    <p class="asat-password-hint-title">{{ __('Password requirements') }}</p>
    <div class="asat-password-checks">
        <span class="asat-password-check" data-password-rule="length">{{ __('8+ characters') }}</span>
        <span class="asat-password-check" data-password-rule="uppercase">{{ __('Uppercase letter') }}</span>
        <span class="asat-password-check" data-password-rule="lowercase">{{ __('Lowercase letter') }}</span>
        <span class="asat-password-check" data-password-rule="number">{{ __('Number') }}</span>
        <span class="asat-password-check" data-password-rule="symbol">{{ __('Symbol') }}</span>
    </div>
</div>
